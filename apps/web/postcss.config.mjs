/** Tailwind v4 tek eklenti kullanır; ayrı tailwind.config.js yok —
 *  tema `app/globals.css` içindeki @theme bloğunda tanımlı. */
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
